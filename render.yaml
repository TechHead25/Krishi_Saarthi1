services:
  - type: web_service
    name: krishi-saarthi-backend
    env: python
    plan: starter
    region: oregon
    branch: main
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: python -m uvicorn frontend:app --host 0.0.0.0 --port ${PORT}
